# Player

AvP has distinct Marine, Predator and Alien player behavior built on shared engine services plus character-specific movement, weapons/attacks, HUD and state logic.

The joypad/input path has exact source-fit coverage and is useful as a reference for alternate input backends.

A native port should translate modern devices into the game's original logical controls rather than moving gameplay semantics into the input layer.
