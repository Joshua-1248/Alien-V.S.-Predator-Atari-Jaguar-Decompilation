# DSP

The DSP side contains the FullSynth music execution path and associated tables used by the shipping game.

The reconstruction preserves the score/event behavior required by the retail build. A PC/audio extraction implementation may execute equivalent host code, but it should preserve original event timing, sample law, envelope behavior, looping and voice-retrigger rules.
