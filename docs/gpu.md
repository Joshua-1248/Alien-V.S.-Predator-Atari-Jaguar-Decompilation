# GPU

The Jaguar GPU is a separate execution domain from the ordinary 68000 program.

Known reconstructed/verified GPU products include the RGB-to-CRY conversion path and LZW-related GPU code used by the retail program. Exact binary/source representations are retained where literal historical source text/tool output cannot be recovered.

Future host implementations should be regression-tested against these GPU semantics rather than changing game-visible behavior.
